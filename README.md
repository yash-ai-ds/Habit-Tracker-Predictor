# Habit-Tracker-Predictor
A lightweight CLI-based habit tracker built with Python and SQLite. Tracks daily completions, streaks, and uses a weighted statistical algorithm to predict your future consistency — no external ML libraries needed.
